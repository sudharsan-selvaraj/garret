export type WindowMode =
  /** Pinned to the desktop level (above wallpaper, behind apps) AND interactive. */
  | 'desktop'
  /** Plain interactive window — used to develop/test widgets. */
  | 'windowed'
