{
  "name": "@garretapp/cli",
  "version": "0.1.1",
  "description": "The garret pack CLI — scaffold, audit, build, and package Garret widget packs.",
  "license": "MIT",
  "author": "Sudharsan Selvaraj",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sudharsan-selvaraj/garret.git",
    "directory": "packages/cli"
  },
  "type": "module",
  "bin": {
    "garret": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/cli.ts --format esm --clean --target node18",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@garretapp/pack-schema": "^0.1.0",
    "archiver": "^6.0.0",
    "esbuild": "^0.21.0"
  },
  "devDependencies": {
    "@types/archiver": "^6.0.0"
  }
}
