node_modules
out
dist
dist-electron
*.log
.DS_Store
.vite
native/build
.electron-gyp
.gyp-tmp
.electron-tmp
.electron-cache

build/icon.iconset
examples/**/bundle.js
examples/**/dist

# transient esbuild bundle from scripts/test-unpack.mjs
._unpack_test_bundle.mjs

# packaged example widget (regenerate via npm run pack:selftest)
examples/**/*.garret

# extension build artifacts
examples/sdk/**/pack/
examples/sdk/**/*.garret

# Example lockfiles may resolve via a corporate proxy — keep them out of the repo
examples/**/package-lock.json

# Fetched-on-build scrcpy server binary (not vendored)
examples/**/host/assets/*.jar
